<new-tutorial 
  :is_admin="<%= current_user.admin?.to_json %>"
  :languages="<%= Language.all.to_json %>"
  :path = "<%= { tutorials: tutorials_path,
                 requests: requests_path,
                 approval: approval_path,
                 new_request: new_request_path
               }.to_json 
          %>"
>
</new-tutorial>
